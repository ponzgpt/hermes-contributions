{
  "name": "hermes-agent-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Landing page for Hermes Agent by Nous Research — a static site you can drive from the keyboard.",
  "scripts": {
    "dev": "npx --yes serve@14 . -l 4321",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run lint && npm run format:check && npm test"
  },
  "devDependencies": {
    "eslint": "^9.18.0",
    "globals": "^15.14.0",
    "jsdom": "^26.0.0",
    "prettier": "^3.4.2",
    "vitest": "^3.0.0"
  }
}
